[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "vite"
  targetPort = 5173
  port = 8888

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  timeout = 60

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
